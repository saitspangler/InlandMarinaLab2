@model InlandMarinaData.Lease

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <h3 class="my-3">Are you sure you want to delete this Lease?</h3>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SlipID)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SlipID)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger me-2" />
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
